# This file is a template for your .env file.
# It lists all the environment variables needed for the application to run.
# Copy this file to .env and fill in the values for local development.
# For production, secrets must be stored in Firebase Secret Manager.

# ==============================================================================
# == PUBLIC VARIABLES (Required for Build)                                  ==
# ==============================================================================
# These variables are exposed to the client-side and are required for the app
# to be built correctly. They are safe to be public.

# --- Firebase Project Configuration ---
# You can find these values in your Firebase project settings under
# "General" > "Your apps" > "SDK setup and configuration".
NEXT_PUBLIC_FIREBASE_API_KEY="YOUR_API_KEY_HERE"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="YOUR_AUTH_DOMAIN_HERE"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="YOUR_PROJECT_ID_HERE"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="YOUR_STORAGE_BUCKET_HERE"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="YOUR_MESSAGING_SENDER_ID_HERE"
NEXT_PUBLIC_FIREBASE_APP_ID="YOUR_APP_ID_HERE"

# --- Telegram Bot Username ---
# The username of your Telegram bot (without the '@' symbol).
# This is used to generate the t.me link for checkout.
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME="CSurgentstudio"


# ==============================================================================
# == SECRET VARIABLES (Required for Local Development)                      ==
# ==============================================================================
# These variables are highly sensitive and are only used for local development.
# For production deployment, you MUST store these in Firebase Secret Manager.
# See the deployment guide in the admin panel for instructions.

# --- Telegram Bot Token ---
# The secret token for your Telegram bot, obtained from BotFather.
TELEGRAM_BOT_TOKEN=""

# --- Google Drive Service Account ---
# The full JSON content of your Google Service Account key file.
# This must be in a single line, enclosed in single quotes.
# e.g., DRIVE_SERVICE_ACCOUNT_JSON='{"type": "service_account", ...}'
DRIVE_SERVICE_ACCOUNT_JSON=''

# --- Google Drive Parent Folder ID ---
# The ID of the Google Drive folder where new project folders will be created.
# This is the last part of the folder's URL.
DRIVE_PARENT_FOLDER_ID=""

# --- Gemini API Key (Optional) ---
# Your API key from Google AI Studio. Required for AI features.
# The app will run without it, but AI features will be disabled.
GEMINI_API_KEY=""